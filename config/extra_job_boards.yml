# Companies for the Ashby, SmartRecruiters, and hand-seeded Greenhouse/Lever
# public job-board APIs.
#
# Ashby and SmartRecruiters have no discovery mechanism, so their companies
# are always curated here by hand. Greenhouse and Lever normally auto-discover
# a company's board the first time one of its postings shows up in another
# curated source — the greenhouse:/lever: sections below are the manual
# escape hatch for a company that never happens to appear that way (e.g. a
# MENA-region company none of the mostly US/EU-focused curated sources ever
# mention), even though its board is sitting right there, publicly pollable.
#
# To add a company, add one line under the right section with its board
# token/company identifier, then verify it works by opening the API URL
# directly in a browser before opening a pull request:
#   Ashby:            https://api.ashbyhq.com/posting-api/job-board/<token>
#   SmartRecruiters:   https://api.smartrecruiters.com/v1/companies/<token>/postings
#   Greenhouse:        https://boards-api.greenhouse.io/v1/boards/<token>/jobs
#   Lever:             https://api.lever.co/v0/postings/<token>?mode=json
#   Workable:          https://apply.workable.com/api/v1/widget/accounts/<slug>?details=true
# No Python changes are needed — scripts/public_sources.py reads this file automatically.

ashby:
  - notion
  - linear
  - ramp
  - deel
  - vercel
  - airtable
  - loom
  - substack
  - attio
  - mercury
  - plaid
  - cohere
  - openai
  - clickhouse
  - thndr  # Cairo, Egypt fintech — verified live 2026-08-18, real Cairo-office postings
  - snowflake  # verified live 2026-08-18, real non-empty board (4.8MB, genuine postings)
  - mercor  # San Francisco, USA (NOT MENA) — AI-data company, mercor.com/careers → "Powered by ashby". Verified 2026-09-06, 96 jobs incl. Infrastructure Engineer (SF/NYC)
  # --- Europe-HQ'd employers (added 2026-09-09) — verified live via verify_extra_boards.py.
  - elevenlabs  # London, UK — voice/audio AI (252 jobs)
  - synthesia   # London, UK — AI video (60; DE / US / NYC)
  - poolside    # Paris, FR — code-generation AI (15; remote EMEA)
  - n8n         # Berlin, DE — workflow automation (43; Berlin)
  - lovable     # Stockholm, SE — AI app builder (84; Stockholm)
  - photoroom   # Paris, FR — AI image editing (17; Paris)
  - pennylane   # Paris, FR — accounting platform (143; Paris)
  - alan        # Paris, FR — health insurance (116; France / Belgium)
  - legora      # Stockholm, SE — legal AI (278; Stockholm / Milan)
  - granola     # London, UK — AI meeting notes (16; London)
  # 2026-09-10 — dev-tools companies confirmed on Ashby (Gemini ATS list, hand-verified).
  - replit      # Foster City, US — browser IDE / AI coding agent (78 postings)
  - posthog     # remote-first (strong EU/UK presence) — open-source product analytics (10 postings)

smartrecruiters:
  - Visa
  - Wise
  - ValU1   # Cairo/Giza fintech (BNPL) — careers.smartrecruiters.com/ValU1; 9 postings incl. Senior Full Stack Developer, Senior Integration Engineer (Sheikh Zayed, EG + Jordan). Confirmed out-of-band 2026-09-10 (SR API can't verify a slug on its own)

greenhouse:
  - careem  # Dubai, UAE super-app (Uber-owned) — verified live 2026-08-18, 231 real postings incl. Dubai/UAE
  # Everything below: real Greenhouse board confirmed live 2026-08-18 (non-empty
  # jobs array, not just a non-404 response) for a company already in
  # config/companies_allowlist.yml whose board had never been auto-discovered —
  # none of these companies ever happened to appear in another curated source
  # first, so discover_job_board_sources() never had a URL to find them from.
  - linkedin
  - datadog
  - databricks
  - mongodb
  - elastic
  - twilio
  - stripe
  - okta
  - figma
  - dropbox
  - asana
  - anthropic
  - deepmind
  - scaleai
  - n26
  - lyft
  - airbnb
  - janestreet
  - coupang
  - baidu
  - jumia
  - tamara  # Riyadh/Dubai fintech (BNPL) — verified live 2026-09-05, 33 real postings incl. Dubai/Riyadh/Cairo
  - block
  - robinhood
  - instacart
  - pinterest
  - reddit
  - roblox
  - epicgames
  - discord
  - duolingo
  - webflow
  # Second verification pass, 2026-08-18: three companies that had transient
  # connection failures in the first pass, retried and confirmed real.
  - cloudflare
  - spacex
  - coinbase
  # --- Europe-HQ'd employers (added 2026-09-09) — widen EMEA / Europe coverage.
  # Each verified live via scripts/verify_extra_boards.py (real non-empty board,
  # sample titles eyeballed). is_software_job() drops the non-engineering rows
  # these broad boards also carry, so only the SWE postings get published.
  - monzo         # London, UK — digital bank (67 jobs on check)
  - gocardless    # London, UK — bank-to-bank payments (Leeds / Lisbon / Riga)
  - celonis       # Munich, DE — process mining (275)
  - getyourguide  # Berlin, DE — travel experiences (53)
  - sumup         # London / Berlin — payments (403)
  - algolia       # Paris, FR / London — search API (32)
  - wayve         # London, UK — autonomous driving (168; DE / UK / JP)
  - raisin        # Berlin, DE — savings marketplace (Berlin / Manchester)
  - bitpanda      # Vienna, AT — crypto brokerage (24)
  - form3         # London, UK — cloud payments infra (remote Europe)
  - hellofresh    # Berlin, DE — meal kits (large board; SWE rows only after filter)
  - scout24       # Berlin, DE — online marketplaces (Berlin)
  - doctolib      # Paris, FR — e-health (125; Paris / Berlin / Frankfurt)
  # 2026-09-10 — allowlisted big-tech company whose careers site is bespoke but
  # is really a Greenhouse board under a non-obvious token.
  - doordashusa   # DoorDash (careersatdoordash.com is a skin over greenhouse:doordashusa) — verified 2026-09-10, 456 postings, board name "DoorDash USA"
  - sonyinteractiveentertainmentglobal   # PlayStation (careers.playstation.com is a skin over this) — verified 2026-09-10, "PlayStation Global", 180 postings incl. AI/UX Software Engineer (US)
  - siei                                 # Sony Interactive Entertainment Inc. — verified 2026-09-10, 62 postings (Tokyo-heavy)

lever:
  # Same discipline: real Lever board confirmed live 2026-08-18 with actual
  # postings, not just a non-404 response. NB: Lever slugs are CASE-SENSITIVE
  # — the token here must match the exact casing in the jobs.lever.co/<slug>
  # URL (e.g. "Bosta", not "bosta").
  - spotify
  - palantir
  - paytm
  - dlocal
  - Bosta  # Cairo, Egypt logistics — verified by hand 2026-09-06 (jobs.lever.co/Bosta, real Cairo eng roles). Case matters.
  - incorta  # Analytics/BI SaaS (San Mateo, US + Riyadh) — verified 2026-09-07 via api.lever.co/v0/postings/incorta, real postings incl. BI Solution Engineer - Riyadh
  # --- Europe-HQ'd employers (added 2026-09-09) — verified live via verify_extra_boards.py.
  - qonto          # Paris, FR — business banking (46 postings)
  - swile          # Paris / Montpellier, FR — employee benefits (27; also Brazil ops)
  - younited       # Paris, FR — consumer credit (Paris / Rome)
  - blablacar      # Paris, FR — carpooling (real Paris backend-engineer roles on check)
  - contentsquare  # Paris, FR — digital-experience analytics (26; Paris / Barcelona / Wrocław)

# ---------------------------------------------------------------------------
# Adding an employer whose board isn't auto-discovered — three paths, easiest
# first. (Regional focus is MENA / Gulf / North Africa, Lane M — but tag each
# entry with its actual city; e.g. Mercor above is San Francisco, not MENA.)
#
# 1. Real Greenhouse/Lever/Ashby board  → add the EXACT-CASE token here
#    (slugs are case-sensitive; verify_extra_boards.py auto-retries variants).
#    Verified & added so far:  Bosta (lever, Cairo) · Mercor (ashby, SF) ·
#    Incorta (lever, San Mateo/Riyadh).
#
# 2. Bespoke careers site, no pollable board  → one row in
#    config/aggregate_links.yml instead ("Browse every role at X"), never a
#    fake posting. Added: Halan (jobs.halan.com, Cairo).
#
# 3. PinpointHQ  → SUPPORTED (fetch_pinpoint_jobs, added 2026-09-06). Add the
#    board under the `pinpoint:` section below: a bare token becomes
#    <token>.pinpointhq.com, a token with a dot is used as a full custom host
#    (e.g. careers.moneyfellows.com). Live: Tabby (Dubai/Riyadh), Money Fellows
#    (Cairo).
#
# 4. Workable  → SUPPORTED (fetch_workable_jobs, added 2026-09-07). Add the
#    apply.workable.com account slug under the `workable:` section below.
#    Live: Foodics, Lucidya, Salla (all Riyadh-HQ'd, regional eng offices).
#
# 5. On BambooHR / Recruitee / Freshteam  → the pipeline has NO fetcher for
#    these yet (Lane M3 / backlog B2), so a token here does nothing until one
#    is built. verify_extra_boards.py already CONFIRMS the first two
#    (2026-09-06: Instabug-"Luciq" ✓ on bamboohr, MoneyHash on recruitee but
#    0 open; Breadfast is on Freshteam). Use
#    `verify_extra_boards.py --dump <platform>:<token>` to see the JSON shape
#    a fetcher must parse.
#
# To check a token:  python scripts/verify_extra_boards.py lever:<Slug>
# A "⚠ couldn't reach …" line = your network blocked it, NOT a missing board.
# ---------------------------------------------------------------------------

pinpoint:
  # PinpointHQ boards. A bare token is a <token>.pinpointhq.com subdomain;
  # a token with a dot is a full custom careers host. Verify with
  #   python scripts/verify_extra_boards.py pinpoint:<token>
  - tabby                      # Dubai/Riyadh BNPL fintech — verified 2026-09-06, 44 postings incl. Senior Fullstack Engineer
  - careers.moneyfellows.com   # Cairo, Egypt fintech (ROSCA app) — verified 2026-09-06, 8 postings

workable:
  # Workable public board — the value is the apply.workable.com account slug
  # (from an apply.workable.com/<slug> careers URL). Verify with:
  #   python scripts/verify_extra_boards.py workable:<slug>
  - foodics                    # Riyadh restaurant-tech/fintech (offices in UAE/Egypt/Jordan/Kuwait) — verified 2026-09-07, 27 postings incl. Senior Backend/Full Stack Engineer
  - lucidya                    # Riyadh AI/CX analytics (remote roles across Cairo/Amman/Istanbul) — verified 2026-09-07, 41 postings incl. Software Engineer
  - salla                      # Jeddah/Makkah e-commerce platform — verified 2026-09-07, 30 postings incl. Senior Backend Engineer, Senior Data Scientist
  - robusta                    # Cairo software studio — verified 2026-09-10, 102 postings
  - blabs                      # (account "b_labs") — verified 2026-09-10, 7 postings
  # Accounts that exist but had 0 open roles — recheck later:
  #   2026-09-07: breadfast · rabbit · elmenus · trella · maxab · capiter ·
  #               nas · mrsool · instashop · yassir · unifonic · zid
  #   2026-09-10: blink22  (Cairo app studio — Workable account exists, 0 open)

recruitee:
  # Recruitee public board — the value is the <slug>.recruitee.com subdomain,
  # taken from a company's Recruitee careers URL. One keyless GET on
  # https://<slug>.recruitee.com/api/offers/. Verify with:
  #   python scripts/verify_extra_boards.py recruitee:<slug>
  - sahl    # Cairo insurtech/payments (sahl.recruitee.com) — verified 2026-09-10, real EG software roles

workday:
  # Big global employers whose careers site is Workday but who never get
  # auto-discovered because no curated source links a *.myworkdayjobs.com URL
  # for them (their community-tracker rows point at the company's own careers
  # page instead). Format: "Company Name | host | site" — Workday has no bare
  # token; each tenant is its own subdomain plus a site path. Verify with:
  #   curl -s -XPOST -H 'Content-Type: application/json' -d '{"limit":5,"offset":0,"searchText":"software engineer"}' \
  #     https://<host>/wday/cxs/<tenant>/<site>/jobs
  # All confirmed live 2026-09-06 with real software-engineer results.
  - Salesforce | salesforce.wd12.myworkdayjobs.com | External_Career_Site   # 527 SWE results
  - NVIDIA | nvidia.wd5.myworkdayjobs.com | NVIDIAExternalCareerSite         # 1719 SWE results
  - Adobe | adobe.wd5.myworkdayjobs.com | external_experienced              # 287 SWE results
  - Visa | visa.wd5.myworkdayjobs.com | Visa                                # 180 SWE results
  - Mastercard | mastercard.wd1.myworkdayjobs.com | CorporateCareers        # 325 SWE results
  - Accenture | accenture.wd103.myworkdayjobs.com | accenturecareers        # verified 2026-09-10, 2000+ postings (IT-services consultancy — is_software_job() filters to the SWE rows)
  - Workday | workday.wd5.myworkdayjobs.com | Workday                       # 153 SWE results
  - Valeo | valeo.wd3.myworkdayjobs.com | valeo_jobs                        # verified 2026-09-07, 1020 total incl. real Cairo posting (AI Engineer, AI4ALL)
  - PwC | pwc.wd3.myworkdayjobs.com | Global_Campus_Careers                 # verified 2026-09-07, 83 total, real campus/early-career postings incl. Egypt (Cairo - ETIC)
