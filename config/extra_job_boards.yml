# Companies for the Ashby and SmartRecruiters public job-board APIs.
#
# Unlike Greenhouse/Lever (auto-discovered from URLs already present in the
# fetched data), these two platforms have no discovery mechanism, so their
# companies are curated here by hand.
#
# To add a company, add one line under the right section with its board
# token/company identifier, then verify it works by opening the API URL
# directly in a browser before opening a pull request:
#   Ashby:            https://api.ashbyhq.com/posting-api/job-board/<token>
#   SmartRecruiters:   https://api.smartrecruiters.com/v1/companies/<token>/postings
# No Python changes are needed — scripts/public_sources.py reads this file automatically.

ashby:
  - notion
  - linear
  - ramp
  - deel
  - vercel
  - airtable
  - loom
  - substack
  - attio
  - mercury
  - plaid
  - cohere
  - openai
  - clickhouse

smartrecruiters:
  - Visa
  - Wise
