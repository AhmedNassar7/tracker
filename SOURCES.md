# Data Sources

Every website, repo, and API `tracker`'s pipeline pulls from, with links. This is a hand-maintained reference — it isn't rebuilt by the hourly pipeline, so update it by hand (see [CONTRIBUTING.md](CONTRIBUTING.md)) whenever a fetcher is added or removed. For what the pipeline *outputs*, see [data/README.md](data/README.md); for career-prep resources aimed at job seekers (not pipeline inputs), see [data/resources.md](data/resources.md).

## Curated layer (`scripts/fetch.py`)

Every row from these sources is checked against [config/companies_allowlist.yml](config/companies_allowlist.yml) (case-insensitive substring match) before publishing.

| Source | Link | What it is |
|---|---|---|
| Remotive | [API](https://remotive.com/api/remote-jobs?category=software-dev) · [site](https://remotive.com/) | Free public API for remote software jobs |
| ArbeitNow | [API](https://arbeitnow.com/api/job-board-api) · [site](https://arbeitnow.com/) | Free public job-board API, strong EU/remote coverage |
| SimplifyJobs — Internships | [repo](https://github.com/SimplifyJobs/Summer2026-Internships) | The most-used community internship tracker on GitHub |
| SimplifyJobs — New Grad | [repo](https://github.com/SimplifyJobs/New-Grad-Positions) | Its new-grad counterpart |
| speedyapply — SWE | [repo](https://github.com/speedyapply/2027-SWE-College-Jobs) | Community new-grad/intern SWE tracker |
| speedyapply — AI | [repo](https://github.com/speedyapply/2027-AI-College-Jobs) | Same tracker family, AI/ML-focused |
| zapplyjobs — New Grad SWE | [repo](https://github.com/zapplyjobs/New-Grad-Software-Engineering-Jobs-2027) | Software-engineering-specific new-grad board |
| zapplyjobs — New Grad (all disciplines) | [repo](https://github.com/zapplyjobs/New-Grad-Jobs-2027) | Broader new-grad board, not limited to SWE |
| zapplyjobs — Internships | [repo](https://github.com/zapplyjobs/Internships-2027) | General internship board |
| zapplyjobs — Data Science | [repo](https://github.com/zapplyjobs/New-Grad-Data-Science-Jobs-2027) | Data science / ML new-grad board |
| zapplyjobs — Canada Jobs | [repo](https://github.com/zapplyjobs/Canada-Jobs-2027) | Canada-focused new-grad board |
| zapplyjobs — Canada Internships | [repo](https://github.com/zapplyjobs/Canada-Internships-2027) | Canada-focused internship board |
| hanzili | [repo](https://github.com/hanzili/canada_sde_junior_new_grad_position) | Canada SDE junior/new-grad tracker |
| ambicuity | [JSON feed](https://jobs.riteshrana.engineer/jobs.json) · [repo](https://github.com/ambicuity/New-Grad-Jobs) | New-grad job list backed by a live JSON API (not README-scraped) |
| LorenzoLaCorte — European Tech | [repo](https://github.com/LorenzoLaCorte/european-tech-internships-2026) | Dedicated Europe internship/new-grad/PhD tracker, added to widen EMEA coverage beyond ArbeitNow. Apply links point at the original LinkedIn posting |
| Lamiiine — visa-sponsored jobs | [repo](https://github.com/Lamiiine/Awesome-daily-list-of-visa-sponsored-jobs) | Actively-updated (daily, 700+ stars) EU-focused list of roles at companies known to sponsor visas/relocation. Same 4-column shape as the generic community-board parser, no separate Age column. Every link confirmed direct-to-ATS (Lever/Greenhouse/Personio/company site). Added 2026-09-07: real yield against the current allowlist is 0 today — same reason as Arbeitsagentur above, the list is dominated by real-but-not-brand-name EU startups/scaleups (BlaBlaCar, TomTom, MBition, Aignostics, roadsurfer, …) that don't overlap this project's FAANG/big-tech-focused allowlist. Kept on the same precedent: correctly built and live, just currently low-yield |
| DereC4 — internships-and-newgrad | [repo](https://github.com/DereC4/internships-and-newgrad) | Personal, actively-updated (80+ stars) deduplicated aggregation. Roughly half its rows are verbatim SimplifyJobs re-exports (same url, SimplifyJobs' own `utm_source=Simplify` params intact) — those safely collapse into the same row as the existing SimplifyJobs fetch via `make_id()`'s company+title+url hash, no duplicate pollution. The rest link straight to real ATS pages (Ashby/Lever/Workday/Greenhouse/Oracle Cloud/amazon.jobs) and cover companies none of the other curated sources do (Marvell, Kroger, Zipline, Hadrian, Hermeus, IXL Learning, Xsolla, …). Known imperfection, accepted rather than fixed: the source itself sometimes lists the identical role under two different Workday career-site paths (different URLs, so `dedupe()` can't catch it) — same tier of noise as other community trackers already in the pipeline. Added 2026-09-07; also required generalizing `clean_cell_text()` in `community_board_parser.py` to strip a markdown link wrapping a cell's own display text, since DereC4 combines title+link in one cell (SimplifyJobs-style) rather than keeping the link in its own column like speedyapply/zapplyjobs/hanzili |
| Amazon (direct) | [API](https://www.amazon.jobs/en/search.json?base_query=software+development+engineer) · [site](https://www.amazon.jobs/) | Amazon's own careers search API, hit directly rather than only through third-party trackers — verified free, keyless, real structured JSON. Every apply link points straight at `amazon.jobs`, with no third-party staleness risk |
| Netflix (direct) | [API](https://netflix.eightfold.ai/api/apply/v2/jobs?domain=netflix.com) · [site](https://explore.jobs.netflix.net/careers) | Netflix's own Eightfold-hosted careers API, hit directly — verified free, keyless, real structured JSON, 500 open roles total. Netflix's own numbered-grade title convention ("Software Engineer 4/5/6") mostly falls outside this project's internship–mid-level scope, so this currently surfaces few rows day to day — real reflection of Netflix's senior-skewed hiring, not a fetcher bug |
| Apple (direct) | [API](https://jobs.apple.com/api/v1/search) · [site](https://jobs.apple.com/en-us/search) | Apple's own careers search API (`POST /api/v1/search`), hit directly rather than only linking out via `config/aggregate_links.yml`. **Keyless** — the CSRF token at `/api/v1/csrfToken` is issued to any client with no auth; `fetch_apple` does that one-call handshake then pages the `filters.keywords:["software engineer"]` + `["internship"]` result sets (~2.4k + ~100 postings, vs ~30k incl. retail). Verified live 2026-09-09. Apple titles carry no level word ("iOS Full-Stack Software Engineer"), so `apple` is in `UNKNOWN_LEVEL_SOURCES` alongside `amazon` — unlabelled titles pass through, `SENIOR_TITLE_RE` still drops Senior/Staff/Principal/Lead. Global feed; the site's Region filter narrows it to EMEA/Europe (London, Cork, Cambridge, Munich, Herzliya offices all appear) |
| Arbeitsagentur (direct) | [API](https://rest.arbeitsagentur.de/jobboerse/jobsuche-service/pc/v6/jobs?was=software+engineer) · [site](https://www.arbeitsagentur.de/jobsuche/) | Germany's Bundesagentur für Arbeit (Federal Employment Agency) own Jobsuche API, hit directly. **Not an officially published third-party API** — access uses a shared client key (`jobboerse-jobsuche`) the agency's own consumer app/site uses internally, reverse-engineered and documented by the open-source [bundesAPI/jobsuche-api](https://github.com/bundesAPI/jobsuche-api) project, not a personally registered credential; could change or break without notice. Each row's `referenznummer` builds a canonical `arbeitsagentur.de/jobsuche/jobdetail/{referenznummer}` URL (confirmed live 2026-09-07) which, for the small/mid German employers that dominate this feed, *is* the actual, sole posting — first-party, not a third-party mirror. Added 2026-09-07: real yield against the current allowlist is near-zero today (the board is dominated by German SMEs with no other digital presence, which barely overlaps this project's brand-name allowlist, and the API has no employer-name filter to target allowlisted companies specifically) — kept anyway on the same precedent as Netflix above: correctly built and live, just currently low-yield, not broken |

## Public layer (`scripts/public_sources.py`)

Widens coverage beyond the allowlist-gated curated layer.

| Source | Link | How it's included |
|---|---|---|
| Greenhouse | [boards-api.greenhouse.io](https://boards-api.greenhouse.io/) | Auto-discovered — a company's board gets polled directly once one of its postings shows up via any other source — **plus ~48 companies hand-seeded** in [config/extra_job_boards.yml](config/extra_job_boards.yml)'s `greenhouse:` section (verified live: real, non-empty boards that had never been auto-discovered). 2026-08-18 batch: LinkedIn, Datadog, Databricks, MongoDB, Elastic, Twilio, Stripe, Okta, Figma, Dropbox, Asana, Anthropic, DeepMind, Scale AI, N26, Lyft, Airbnb, Jane Street, Coupang, Baidu, PhonePe, Jumia, Block, Robinhood, Instacart, Pinterest, Reddit, Roblox, Epic Games, Discord, Duolingo, Webflow, Cloudflare, SpaceX, Coinbase, Careem. **2026-09-09 Europe batch** (widen EMEA / Europe coverage): Monzo, GoCardless, Celonis, GetYourGuide, SumUp, Algolia, Wayve, Raisin, Bitpanda, Form3, HelloFresh, Scout24, Doctolib. **2026-09-10:** DoorDash (`greenhouse:doordashusa`, 456 postings), **PlayStation** (`greenhouse:sonyinteractiveentertainmentglobal` = "PlayStation Global", 180; + `greenhouse:siei` = "Sony Interactive Entertainment Inc.", 62 — both skinned by `careers.playstation.com`) |
| Lever | [api.lever.co](https://api.lever.co/) | Auto-discovered, same mechanism as Greenhouse — plus hand-seeded companies (Spotify, Palantir, Paytm, dLocal, Bosta, Incorta; **2026-09-09 Europe batch:** Qonto, Swile, Younited, BlaBlaCar, Contentsquare), same discipline |
| Workday | CXS API (per-tenant, no single URL) | Auto-discovered, same mechanism as Greenhouse — plus hand-seeded tenants in `config/extra_job_boards.yml`'s `workday:` section (`Company \| host \| site`), incl. Salesforce, NVIDIA, Adobe, Visa, Mastercard, Valeo, PwC, **Accenture** (2026-09-10) |
| Ashby | [api.ashbyhq.com](https://api.ashbyhq.com/) | Listed by hand in [config/extra_job_boards.yml](config/extra_job_boards.yml) — verify a token with `curl https://api.ashbyhq.com/posting-api/job-board/<token>` before adding. Includes Snowflake and Thndr (Cairo, Egypt), verified live 2026-08-18. **2026-09-09 Europe batch:** ElevenLabs, Synthesia, Poolside, n8n, Lovable, Photoroom, Pennylane, Alan, Legora, Granola. **2026-09-10:** Replit, PostHog (dev-tools, from a Gemini ATS list, hand-verified) |
| SmartRecruiters | [api.smartrecruiters.com](https://api.smartrecruiters.com/) | Listed by hand in [config/extra_job_boards.yml](config/extra_job_boards.yml) — the API can't confirm a slug is valid, so only add ones verified out-of-band. **ValU** (`ValU1`, Cairo/Giza BNPL fintech — 9 postings, confirmed 2026-09-10 via a real careers URL + job slug) |
| PinpointHQ | `<host>/postings.json` (per-tenant) | Listed by hand in [config/extra_job_boards.yml](config/extra_job_boards.yml)'s `pinpoint:` section — a bare token is a `<token>.pinpointhq.com` subdomain, a token with a dot is a custom careers host. Added the MENA push's Pinpoint tenants: **Tabby** (Dubai/Riyadh BNPL, 44 postings) and **Money Fellows** (`careers.moneyfellows.com`, Cairo). Verify with `python scripts/verify_extra_boards.py pinpoint:<token>` (or `--dump` for its JSON shape) |
| Workable | [apply.workable.com](https://apply.workable.com/) (widget API, per-account) | Listed by hand in [config/extra_job_boards.yml](config/extra_job_boards.yml)'s `workable:` section — the value is the `apply.workable.com/<slug>` account slug. One keyless GET on `api/v1/widget/accounts/<slug>?details=true` returns every posting with its full HTML JD inline. Seeded: **Foodics** (Riyadh restaurant-tech, 27 postings), **Lucidya** (Riyadh AI/CX, 41), **Salla** (Jeddah e-commerce, 30); 2026-09-10: **robusta** (Cairo software studio, 102), **b_labs** (7). Verify with `python scripts/verify_extra_boards.py workable:<slug>` (or `--dump` for its JSON shape) |
| Recruitee | `<slug>.recruitee.com/api/offers/` (per-account) | Listed by hand in [config/extra_job_boards.yml](config/extra_job_boards.yml)'s `recruitee:` section — the value is the `<slug>.recruitee.com` subdomain. `fetch_recruitee_jobs` does one keyless GET returning `{offers:[…]}` (published rows only, full HTML `description`+`requirements`, `remote` flag, `locations[]`). Added 2026-09-10 for **sahl** (Cairo insurtech/payments). Verify with `python scripts/verify_extra_boards.py recruitee:<slug>` |
| Devpost | [devpost.com/api/hackathons](https://devpost.com/api/hackathons) · [site](https://devpost.com/hackathons) | Devpost's own JSON API (the hackathons page itself is client-rendered) |
| Unstop | [API](https://unstop.com/api/public/opportunity/search-result?opportunity=hackathons) · [site](https://unstop.com/hackathons) | Free, keyless, paginated JSON API — 6,000+ hackathons at time of adding, filtered to `oppstatus=recruiting` (still accepting registrations). Complements Devpost rather than duplicating it |
| Devfolio | [API](https://api.devfolio.co/api/hackathons) · [site](https://devfolio.co/hackathons) | Free, keyless JSON API, strong for Web3/student hackathons Devpost under-covers. Filtered client-side to events whose `ends_at` hasn't passed yet, since the API has no separate "still open" flag |
| HackerEarth | [`/chrome-extension/events/`](https://www.hackerearth.com/chrome-extension/events/) · [site](https://www.hackerearth.com/challenges/) | HackerEarth's own keyless JSON endpoint (the one its browser extension calls). Small, high-signal set of currently-featured hackathons + hiring challenges — notably stronger on MENA/India events the other three under-cover. `url` is the real event page; not disallowed by `robots.txt`. Closed events dropped by `end_utc_tz` in the past |
| Luma | [luma.com/discover](https://luma.com/discover) | Filtered for tech/software relevance — Luma's discover page is a general community directory, not tech-specific |
| Curated events | [config/events.yml](config/events.yml) | Hand-maintained list of tech/career events (conferences, summits, career fairs — Techne Summit, RiseUp, GITEX, LEAP, STEP, Web Summit, plus a Europe/EMEA block added 2026-09-09: Slush, VivaTech, Devoxx Belgium, London Tech Week, WeAreDevelopers, Bits & Pretzels, GITEX Europe, Junction; an OSS/dev-community + company-hosted block added 2026-09-11: DjangoCon US, Django Day Copenhagen, FOSDEM, All Things Open, Open Source Summit Europe, KubeCon + CloudNativeCon North America, GitHub Universe, Grace Hopper Celebration, First Gens in Tech, Google Cloud Summit Doha, TechCrunch Disrupt, Microsoft Ignite, AWS re:Invent) that no pollable API covers. One line each: `Name \| Organizer \| City, Country \| YYYY-MM-DD \| URL`. Past-dated rows drop out automatically; for an annual event, bump the date to next year's edition when announced |
| confs.tech (conference-data) | [github.com/tech-conferences/conference-data](https://github.com/tech-conferences/conference-data) — one JSON array per topic per year under `conferences/<year>/<topic>.json`, served off `raw.githubusercontent.com` | **Added 2026-09-11** (`fetch_confs_tech_events` in `public_sources.py`). Keyless, actively-maintained, crowd-sourced conference database — exactly the "check for a JSON API before scraping" case this repo prefers, same CDN pattern already trusted for the curated-layer community trackers. Polls `CONFS_TECH_TOPICS` (24 software-engineering-relevant topics — python, javascript, java, devops, security, opensource, … — non-engineering topics like leadership/product/ux/accessibility/iot/css are deliberately left out) for the current year + next year. A topic file not yet backfilled for a given year 404s and is skipped quietly rather than logged as a failure (2027 currently only has ~14 of the 24 topics). Deduplicated against `config/events.yml` by normalized name so a conference hand-seeded there (e.g. KubeCon) isn't published twice |

## Considered, not added

- **workopia/UK-Graduate-Jobs** and its sibling country repos (France, Germany, Spain, Netherlands, etc.) — a large, actively-updated family of graduate-job trackers. Skipped because their apply links point at Workopia's own site (an intermediary/lead-gen page), not the employer's original posting, which breaks this repo's "every row links straight to the real application page" rule — and the table is HTML, not a plain pipe-table, so it wouldn't reuse the existing generic parser.
- **jobright-ai/2026-Software-Engineer-New-Grad** — actively maintained, but every apply link routes through `jobright.ai/jobs/info/<id>?utm_campaign=…` (its own tracking redirect), not the employer's page — same rule as workopia. Also caps its list to "the last 7 days" and pushes users to a paid Airtable link for the rest.
- **Non-standard ATSes surfaced by the MENA push** — the region's employers cluster on ATSes the pipeline didn't originally read. Each has a public keyless JSON API and follows the same pattern as the existing fetchers; `scripts/verify_extra_boards.py` already has a checker for each (`platform:token`), so verify the endpoint shape (`--dump platform:token`) before building:
  - **PinpointHQ** — `{company}.pinpointhq.com/postings.json` (or a custom domain like `careers.moneyfellows.com`). **Built 2026-09-06** (`fetch_pinpoint_jobs` in `public_sources.py`) — now a supported public-layer source, see the table above. First tenants: **Tabby** (`tabby.pinpointhq.com`, 44 postings incl. "Senior Fullstack Engineer"), **Money Fellows** (`careers.moneyfellows.com`, 8, Cairo).
  - **Workable** — `apply.workable.com/api/v1/widget/accounts/{slug}?details=true`. **Built 2026-09-07** (`fetch_workable_jobs` in `public_sources.py`) — now a supported public-layer source, see the table above. First accounts: **Foodics** (Riyadh, 27), **Lucidya** (Riyadh, 41), **Salla** (Jeddah, 30). Accounts that exist but had 0 open roles at check time (recheck later): Breadfast, Rabbit, Elmenus, Trella, MaxAB, Capiter.
  - **BambooHR** — `{company}.bamboohr.com/careers/list` (`{"result": […]}`). Seen: **Instabug / "Luciq"** (`instabug.bamboohr.com`, ✓ but only 1 non-software opening at check time). Not built yet (Lane M3).
  - **Recruitee** — `{company}.recruitee.com/api/offers/` (`{"offers": […]}`). Seen: **MoneyHash** (`moneyhash.recruitee.com`, valid board, 0 open when checked). Not built yet (Lane M3).
  Added so far via routes we *do* support: **Foodics**, **Lucidya**, **Salla** (`workable:`), **Tabby** & **Money Fellows** (`pinpoint:`), **Mercor** (`ashby:mercor`, 96 jobs — San Francisco, **not** MENA), **Bosta** (`lever:Bosta`, Cairo).
- **Microsoft first-party fetcher** — evaluated 2026-09-09 (B7). `jobs.careers.microsoft.com` is an Eightfold-hosted SPA; the real data API is on a separate host, `gcsservices.careers.microsoft.com`, which from this environment serves a TLS certificate that doesn't validate for that hostname (and `microsoft.eightfold.ai` 403s). Can't hand-verify a keyless contract through that, and the repo rule forbids committing an unverified source — so Microsoft stays on the `aggregate_links.yml` "browse every role" row until it can be verified from a clean network.
- **Meta first-party fetcher** — evaluated 2026-09-09 (B7) and **rejected on source terms**, not capability. `metacareers.com/robots.txt` opens with "Collection of data on Facebook through automated means is prohibited unless you have express written permission" and disallows `/*cursor=` (the jobs pagination param) and `/ajax/`. Same "respect source terms" rule that keeps LinkedIn to the single-id guest endpoint. Meta stays an `aggregate_links.yml` row. (Apple, evaluated the same day, had **no** such prohibition — no `robots.txt` at all on `jobs.apple.com` — and a keyless JSON API, so it was built; see the curated-layer table above.)
- **GitHub / Arm / Cisco / Splunk / Shopify / Bloomberg first-party boards** — evaluated 2026-09-10. None run a keyless JSON board: `github.careers` is **iCIMS** (soft-404-prone, no keyless enumeration — same family this repo dropped vanshb03 over), `careers.arm.com` is **Radancy/TalentBrew** (its `search-jobs/results` endpoint returns rendered HTML fragments, not structured JSON), `careers.cisco.com` (and the Splunk section under it) is **Phenom** (GraphQL behind a tenant handshake), `shopify.com/careers` is bespoke (a Gemini ATS list claimed Ashby — `ashby:shopify` 404s), `bloomberg.avature.net` is **Avature** (no public keyless API). Each got a hand-verified `config/aggregate_links.yml` "browse every role" row instead. **PlayStation** and **Accenture**, checked the same day, *were* reachable — PlayStation is Greenhouse under `sonyinteractiveentertainmentglobal` / `siei`, Accenture is Workday (`accenture.wd103.myworkdayjobs.com` / `accenturecareers`, 2000+ postings) — so both went into `extra_job_boards.yml`. Pinterest / Mastercard / OpenAI in the same batch were already covered (`greenhouse:pinterest`, `workday:Mastercard`, `ashby:openai`). DoorDash, checked the same day, *was* reachable — a Greenhouse board under the non-obvious token `doordashusa` (456 postings) — so it went into `extra_job_boards.yml`. (Also from that list: `ashby:replit` and `ashby:posthog` verified and added; `ashby:linear` / `ashby:ramp` were already in; `greenhouse:hubspot` is a valid but empty board, not added.)
- **Ericsson** — evaluated 2026-09-10. `jobs.ericsson.com` is Eightfold, but its `api/apply/v2/jobs` returns **HTTP 403** (bot-protected) — can't enumerate keyless. **Booking.com** — its `jobs.booking.com` careers site is **iCIMS**. Both got `config/aggregate_links.yml` rows. **Atos** (`jobs.atos.net`, SuccessFactors) likewise.
- **The Workable "partners" directory** (`partners.workable.com` — Hackajob, Haystack, Jobbio, Welcome to the Jungle / Otta, Tempo, The Hub) — these are third-party job *marketplaces / aggregators*, not employer ATS boards. Every one routes applies through its own funnel, and Otta was already rejected on that exact basis (see "Niche developer-focused job boards" below). Not sources.
- **LinkedIn company job pages** (`linkedin.com/company/<x>/jobs`) — enumerating a company's jobs from LinkedIn means scraping against its bot protection at volume, which this repo's "respect source terms" rule forbids (LinkedIn is used *only* via its unauthenticated guest job-posting endpoint, read-only, one id at a time, for the closed-link check in `scripts/net.py`). It's also unnecessary: a company's LinkedIn listings are cross-posts of its own ATS board — so fetching the Greenhouse/Lever/Ashby/Workday board (or an `aggregate_links.yml` row for a bespoke careers site) gets the same roles from a keyless, allowed source. The tell for which ATS: the "Powered by …" line in the company careers page footer.
- **General consumer job boards — Wuzzuf, Bayt, LinkedIn Jobs, Indeed, Glassdoor, StepStone, GulfTalent, Naukrigulf** — evaluated 2026-09-07 after a request to widen MENA/EMEA coverage through these. Rejected for the same reason as the LinkedIn company-pages entry below, confirmed per-site instead of assumed:
  - **Wuzzuf** — `robots.txt` explicitly disallows the exact URL shapes needed (`Disallow: /*filters`, `/*?a=`, `/*?o=`, `/*?l=`, `/*?t=` — covering both its filtered search pages and individual job-posting links) and explicitly names `ClaudeBot` in its disallow list. No JSON API backs the search pages either — the HTML is server-rendered with no `__NEXT_DATA__`/API reference to key off.
  - **Bayt** — `robots.txt` disallows `*filters[` and all of `/en/jobs/*-jobs/` (its search-results path) for every crawler, and carries a blanket `Disallow: /` naming `LinkedInBot` and `IndeedBot` specifically. Its advertised "REST JSON Jobs" feed is a paid partner integration, not a keyless API.
  - **LinkedIn** — even a single bare request for `robots.txt` returned a Google reCAPTCHA challenge page. No path to polling this without solving CAPTCHAs at scale.
  - **Indeed** — the public "Job Search API" was discontinued; what remains requires Publisher/Partner Program approval, not a keyless call.
  - **Glassdoor** — no public API of any kind; even `robots.txt` is served from behind Cloudflare with a redirect, consistent with heavy bot-protection on the actual job pages.
  - **StepStone** — its job-feed API is a paid XML jobfeed arrangement, not free/keyless.
  - **GulfTalent, Naukrigulf** — same category (major commercial job portals monetizing search traffic); no public JSON API found, and the ToS pattern above (disallowing other job boards' bots, gating real feeds behind partner deals) is standard for this class of site.
  All of these are consumer job-search engines that monetize aggregated listings, structurally unlike Greenhouse/Lever/Ashby/Workday (ATS platforms whose employer customers *want* their own postings polled). Scraping them would mean fragile HTML parsing against sites that explicitly disallow it, with real risk of IP/rate-limit bans on the shared GitHub Actions runners. The legitimate path to more MENA / EMEA / Europe coverage is what's already working — hand-seeding a company's real ATS board (MENA: Careem, Thndr, Bosta, Mercor, Incorta, Foodics, Lucidya, Salla; Europe, 2026-09-09: Monzo, GoCardless, Celonis, SumUp, Wayve, Doctolib, Qonto, BlaBlaCar, ElevenLabs, Pennylane, Alan, Legora + more — see `scripts/verify_extra_boards.py --europe`) or building the remaining BambooHR/Recruitee fetchers above (Lane M3), since a company posting to Wuzzuf/Bayt usually has a real ATS underneath.
- **Niche developer-focused job boards — Honeypot.io, Otta.com, Cord.co** — evaluated 2026-09-07 alongside the general boards above.
  - **Honeypot.io** and **Otta.com** — both domains hang/timeout on every connection attempt (TLS connects, then nothing). Consistent with their actual fates: Honeypot was acquired and wound down around 2023; Otta was acquired by Welcome to the Jungle in 2024 and folded into that product. Neither has a live site to fetch from.
  - **Cord.co** (redirects to `cord.com`) — its JS bundle (`assets.co-hire.com/react/p/static/*.js`) shows the whole product is gated behind authenticated `/api/v2/account/candidate/...` endpoints (`onboarding/like`, `onboarding/skip`, `candidate/feed`) — a Tinder-style swipe feed for logged-in candidates, not a browsable public job board. No public "list open jobs" endpoint exists to poll.
- **RemoteOK and We Work Remotely** — evaluated 2026-09-07 as candidates to widen global/remote coverage. Both are real, live, free, keyless APIs (RemoteOK: `remoteok.com/api`, JSON; We Work Remotely: `weworkremotely.com/categories/remote-programming-jobs.rss`, RSS) — but rejected for the same rule that already excludes workopia and jobright-ai: **every apply link routes through the aggregator's own page, never the employer's.** Checked directly: RemoteOK's `url` and `apply_url` fields both point at `remoteok.com/remote-jobs/<slug>`; We Work Remotely's RSS `<link>` points at `weworkremotely.com/remote-jobs/<slug>`. Neither exposes the underlying employer application URL anywhere in its feed. RemoteOK's own API terms also ask for backlink attribution "or they'll suspend access" — a normal ask for their own site, but a sign the relationship is "traffic funnel back to RemoteOK," a different posture than Greenhouse/Lever/Ashby (whose employer customers want the raw postings polled with no strings attached). Neither site is disreputable — this is a data-shape rejection, not a trust one — but it means adding them would introduce the one thing this repo has twice already refused: a row that looks identical to every other row in the table but silently takes the user to a middleman page instead of the real application.
- **EURES** (European Job Mobility Portal, EU-wide, 31 countries) — evaluated 2026-09-07 alongside the German Arbeitsagentur integration above. Its job-search API (`europa.eu/eures/api/jv-searchengine/public/jv-search/search`) is real, live, and fully keyless (confirmed with a live POST returning 255K+ matching records) — but like Arbeitsagentur, it's an unofficial reverse-engineered API, not a published one ([rorar/EURES-API-Documentation](https://github.com/rorar/EURES-API-Documentation)). Unlike Arbeitsagentur, though, there's no clean canonical URL to build: the real originating link is buried as a raw HTML `<a href>` inside a free-text `applicationInstructions` field on the job-detail endpoint (`jv-searchengine/public/jv/id/{id}`), and its shape differs per contributing national employment service — a Swedish listing's link went to Sweden's own "Platsbanken" board, itself one more hop from the employer, with no shared schema across the other 30 countries to build a general parser against. Deferred rather than rejected outright: buildable in principle, but would need per-country research into each PES's `applicationInstructions` format before it could be reliable, a materially bigger lift than every other source's fixed JSON contract.
- **huzzle-app/Graduate-Jobs-with-Visa-Sponsorship-UK** — evaluated 2026-09-07. Last pushed 2024-04-24 — over two years stale at time of check. Same reasoning as vanshb03's removal: a job-listing repo that hasn't been touched in years is, in practice, all dead/expired links by now. Not pursued.
- **shubheksha/companies-sponsoring-visas** — evaluated 2026-09-07. Different in kind from the above (a hand-maintained table of *which companies* sponsor visas — Stripe, Shopify, Datadog, Klarna, … — not job postings), also last pushed 2024-03-28 and the file itself warns "please verify this information as it may not always be fully up-to-date." **Not usable the way it was proposed** (a static `config/visa_sponsors.yml` that blanket-tags every posting from a listed company as `visa_sponsorship: true`) — that's the exact fabrication this repo's B4 feature deliberately avoids: `visa_sponsorship` is set by `patterns.py`'s `detect_requirements()` **only when a specific posting's own description text says so**, precisely because a company's general policy doesn't mean every role/location/level it posts is actually sponsored. A stale, unverified, company-wide flag would be a real regression from that standard, not an enhancement. Not pursued.
- **visajobs.xyz** — evaluated 2026-09-07. A real site with real content — checked a live country page (`/jobs-in-germany/`) and every link goes straight to the employer's own careers page (e.g. `adjoe.io/company/careers/?j_id=...`), passing the direct-link rule. But it's a static-site-generated collection of individual HTML pages (per-country/per-role), not a markdown table or JSON API — would need a bespoke HTML scraper rather than reusing the existing generic parser, and its total volume ("320+ roles") is modest. Not built now; a candidate to revisit if someone wants to write that scraper.
- **forgeapply/internships-and-new-grad-jobs** — evaluated 2026-09-07. Actively updated (60,000+ postings claimed, daily refresh), but every single apply link routes through `forgeapply.com/j/<slug>?utm_source=github&utm_medium=referral&utm_campaign=early_career_repo` — their own site, never the employer's — same rule that already excludes workopia, jobright-ai, RemoteOK, and We Work Remotely.
- **Ploy's UK technology application trackers** (`get.ploy.club/opentracker/uk-technology-application-tracker-{internships,graduates}`) — evaluated 2026-09-07. Both are Framer-built marketing pages for the Ploy app; the actual job table isn't in the page at all — it's rendered from an embedded Notion database (`notion.site`). Notion has no public JSON API for this, so pulling it would mean reverse-engineering Notion's private internal API — same risk category as the EURES/Arbeitsagentur "unofficial API" tradeoff below, but with a much smaller payoff (one UK-only niche list) and a stricter ToS posture. Not pursued.
- **Third-party hackathon aggregators — Hackalist, 0xarchit/hackathon-api, dev-shetty/hackathon-finder, lorenzopalaia/HackTrack-EU, TeeWrath/NoteXplorer-API** — evaluated 2026-09-07 as ways to widen event coverage beyond Devpost/Unstop/Devfolio. None added, checked individually:
  - **Hackalist** (`hackalist.github.io`) — the claimed `/api/1.0/{year}/{month}.json` endpoint 404s for every month now; the site redirects to `www.hackalist.org` with no working API, and its own page `<meta>` describes it as *"An archive of hackathons from around the world (2014-2025)"*. The GitHub repo's `api/1.0/` folder stops at `2025` and was last pushed 2026-04-09. Dead for live data — no current or future events.
  - **0xarchit/hackathon-api**, **dev-shetty/hackathon-finder**, **TeeWrath/NoteXplorer-API** — each is one person's hosted scraper that re-aggregates Devpost / Unstop / Devfolio / MLH. Two problems: (1) they mostly re-serve platforms this pipeline already polls **directly**, adding a middleman hop for links that should point at the real event; (2) the hourly job would then depend on a hobby project's free Vercel/Render deployment staying up — a fragility this repo doesn't accept for any other source. 0xarchit last pushed 2025-12-23.
  - **lorenzopalaia/HackTrack-EU** — genuinely active (pushed 2026-09-06) and EU-focused, but same two objections: personal `hacktrack-eu.vercel.app` hosting, and it's a scraper layer over the same platforms rather than a primary source.
  - The real gap these point at is **MLH (Major League Hacking)** season/member events, which Devpost/Devfolio/Unstop under-cover — but MLH has no public JSON API (its `/seasons/{year}/events` page 302-redirects bots, carries no `ld+json`, and no maintained community JSON mirror exists), so it's the same "HTML scrape against bot protection" this repo refuses elsewhere. `config/events.yml` remains the hand-curated escape hatch for a specific must-have event.
- **Second batch, evaluated 2026-09-07** (awesome-lists + GitHub topic API + HackerEarth):
  - **mbiesiad/awesome-hackathons**, **dribdat/awesome-hackathon** — markdown "awesome lists" of recurring hackathon *brands* (NASA Space Apps, Hash Code, Call for Code, Hacktoberfest…), no per-event dates, last pushed 2024-05, and some entries already dead (Google Code Jam was discontinued 2023). Not a data feed — for a specific recurring event, one dated `config/events.yml` line is the right call.
  - **GitHub topic API** (`topic:hackathon` / `hackathon-template`) — real keyless REST API, but repos with those topics are *project submissions*, not event listings with dates/links. Pure noise for this pipeline.
  - **jdorfman/awesome-json-datasets**, **sharmadhiraj/free-json-datasets** — generic public-API directories, nothing to do with events.
  - **HackerEarth** — **added** (`fetch_hackerearth_hackathons`, 2026-09-07). Its `/chrome-extension/events/` endpoint is keyless JSON, `url` points at the real event page, and it's not `robots.txt`-disallowed — same category as the Devpost/Unstop/Devfolio internal APIs. Small set (~6 featured events) but high-signal and MENA/India-heavy. See the table above.
- **vanshb03/{Summer2027-Internships, New-Grad-2027}** — *was* a source; **removed 2026-09-06**. In practice it was the single biggest contributor of dead / "job not found" links: its rows skew heavily toward soft-404-prone hosts (joinbytedance.com, jobs.apple.com, metacareers.com, iCIMS), its Amazon links carried job ids ~3,000,000 (years stale — current ids are ~10,500,000), and every Microsoft row pointed at one generic `apply.careers.microsoft.com/careers?query=intern…` search URL rather than a real posting. Everything it covered is already pulled fresher from a live source (Amazon via its own API; Adobe/Roblox/Stripe/Duolingo/Pinterest via their Greenhouse/Ashby/Workday boards; Google/Meta/Apple/Microsoft via `config/aggregate_links.yml` + SimplifyJobs).

## Notes

- Config-file changes (`companies_allowlist.yml`, `extra_job_boards.yml`) don't need a new row here — this file tracks *where the data comes from*, not which companies are accepted.
- If a fetcher is added or removed in `scripts/fetch.py` or `scripts/public_sources.py`, update this file and the "How it works" section in [CONTRIBUTING.md](CONTRIBUTING.md) in the same change.
